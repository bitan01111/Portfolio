# SEO

The portfolio is a single `index.html` page with:

- Basic metadata: `title`, `description`, `canonical`
- OpenGraph tags for social sharing
- Twitter card tags
- JSON-LD structured data (`@type: Person`)

Recommendation:
- Add `twitter:site` and `twitter:creator` for further completeness (optional).

